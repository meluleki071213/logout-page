document.getElementById("logoutButton").addEventListener("click", function () {
  // Perform logout functionality here (e.g., clearing session, redirecting to login page)

  // Redirect to login page
  window.location.href = "dinersclub:/";
});
